Execute os comandos a seguir dentro da pasta `src` do Servidor e do Cliente.

## Compilação

```sh
javac -cp . com/authentication/model/*.java
```

```sh
javac -cp . com/authentication/rmi/*.java
```

## Execução
```sh
java -cp . com/authentication/rmi/Main
```
